/*
*********************************************************************************************************
*
*   模块名称 : SRAM 硬件驱动层
*   文件名称 : bsp_sram.c
*   说    明 : 负责 SRAM 的硬件初始化。
*
*              使用 STM32CubeMX 生成的项目中，FSMC 已由 MX_FSMC_Init() 自动初始化，
*              BSP_SRAM_HwInit() 可以为空或直接调用 MX_FSMC_Init()。
*
*              若在非 CubeMX 项目或其他平台上使用，在此函数中手动配置 FSMC/总线。
*
*   移植步骤 :
*       1. 修改 bsp_sram.h 中的地址和尺寸宏
*       2. 在 BSP_SRAM_HwInit() 中实现新平台的总线初始化
*       3. 修改 SRAM_READ8 / SRAM_WRITE8 宏为新平台的读写方式
*       4. bsp_sram_port.c 和 bsp_sram_test.c 无需任何修改
*
*********************************************************************************************************
*/

#include "bsp_sram.h"

/* CubeMX 生成的 FSMC 初始化函数声明（位于 fsmc.h）*/
extern void MX_FSMC_Init(void);

/*
*********************************************************************************************************
*   函 数 名: BSP_SRAM_HwInit
*   功能说明: SRAM 硬件初始化
*             CubeMX 项目中 FSMC 已在 main() 里由 MX_FSMC_Init() 初始化，
*             此函数保留作为统一入口，方便非 CubeMX 项目在此填写初始化代码。
*   形    参: 无
*   返 回 值: 无
*********************************************************************************************************
*/
void BSP_SRAM_HwInit(void)
{
    /*
     * CubeMX 项目：MX_FSMC_Init() 已在 main() 中调用，此处无需重复。
     *
     * 非 CubeMX 项目：在此配置 FSMC，例如：
     *
     *   __HAL_RCC_FSMC_CLK_ENABLE();
     *   // 配置 GPIO（D0~D15, A[0~12+], NOE, NWE, NE3, NBL0, NBL1）
     *   // 配置 FSMC_NORSRAMInitTypeDef ...
     *   // HAL_SRAM_Init(...);
     *
     * 其他平台（如 NXP / GD32 等）：在此实现对应的总线初始化。
     */
}
